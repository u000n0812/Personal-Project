# --- 핵심 의존성 (항상 필요) ---
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0

# --- 데이터 수집 ---
yfinance>=0.2          # 온라인 가격 수집 (네트워크 allowlist 필요)

# --- 통계 모델 ---
statsmodels>=0.14      # ARIMA
arch>=6.0              # GARCH (변동성)

# --- 머신러닝 ---
xgboost>=2.0

# --- 딥러닝 (선택) ---
# LSTM 모델을 사용하려면 torch를 추가로 설치하세요.
# torch>=2.0
