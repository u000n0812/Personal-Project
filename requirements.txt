# 사내 지침문서 어시스턴트 - Python 의존 라이브러리
# 기본 구성: Embedding과 LLM을 모두 Ollama(로컬)에서 실행한다.
# 외부 AI API 라이브러리는 포함하지 않는다.

# --- UI ---
streamlit>=1.30

# --- 문서 텍스트 추출 ---
pypdf>=4.0             # PDF
cryptography>=41.0     # 암호로 보호된 PDF(AES)를 열 때 pypdf가 필요로 함
python-docx>=1.1       # DOCX
openpyxl>=3.1          # XLSX (선택)
python-pptx>=0.6.23    # PPTX (선택)

# --- 계산 ---
numpy>=1.24
# faiss-cpu>=1.8      # (선택) 문서 수가 매우 많을 때 검색 가속

# Embedding: Ollama의 bge-m3 를 사용하므로 별도 pip 설치가 필요 없다.
#   ollama pull bge-m3
# Ollama 대신 sentence-transformers를 쓰려면:
#   pip install -r requirements-sentence-transformers.txt
